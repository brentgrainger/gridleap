export const appSchemaGenerator = (schema) => {
      // Logic to transform schema into app JSON schema
      const appSchema = {}; // Transformed app schema logic here
      return appSchema;
    };
